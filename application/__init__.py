"""universal-chat-agent — the brain. A-Boxed Level 1 application package.

Semantic-prefix layers (Python underscore form of the L1 hyphen convention):
  api_*       presentation + external integrations
  business_*  orchestration + pure domain rules
  data_*      persistence
  (no prefix) cross-cutting: configuration, logger, middleware, common
"""
